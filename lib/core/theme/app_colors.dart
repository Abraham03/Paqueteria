import 'package:flutter/material.dart';

class AppColors {
  // Colores de Marca
  static const Color primary = Color(0xFF0F172A);   // Azul Oxford Profundo (Moderno)
  static const Color accent = Color(0xFF3B82F6);    // Azul Eléctrico (Acciones)
  static const Color highlight = Color(0xFFF59E0B); // Ámbar (Alertas/Pendientes)
  
  // Colores de Superficie
  static const Color background = Color(0xFFF8FAFC); // Gris ultra claro (Fondo)
  static const Color surface = Colors.white;         // Tarjetas
  static const Color error = Color(0xFFEF4444);      // Rojo suave
  static const Color success = Color(0xFF10B981);    // Verde Esmeralda
  
  // Colores de Texto
  static const Color textPrimary = Color(0xFF1E293B); // Casi negro
  static const Color textSecondary = Color(0xFF64748B); // Gris Slate para subtítulos
  static const Color textOnPrimary = Colors.white;
}